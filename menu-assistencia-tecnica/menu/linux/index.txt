import os
import subprocess

def executar_script(nome_funcao):
    caminho_script = os.path.join(os.path.dirname(__file__), nome_funcao, "index.sh")
    
    if not os.path.isfile(caminho_script):
        print(f"Script não encontrado: {caminho_script}")
        return

    print(f"Executando script: {caminho_script}")
    subprocess.run(["bash", caminho_script])

# Exemplo de uso
if __name__ == "__main__":
    executar_script("informacao_sistema")
